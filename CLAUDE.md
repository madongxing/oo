# 叙引 (Xuyin) — 项目说明（CLAUDE.md · v3.1）

> 存为仓库根目录 `CLAUDE.md`，Claude Code 每次会读它。
> **v3 较 v2.1（2026-06-13 文档收敛）：** ① 指向《现行真相源索引》为唯一 canon 指针；② 补回 A 代「认知栈 + 工程规格③链」；③ 创作铁律 6/7 重锚七问规范 v1.1；④ P0 收编七问规范 §7；⑤ 台数统一为 4 台。
> **v3.1 较 v3（2026-06-29 对齐 v2.24）：** ① 索引指针 v1.0→**v2.24**；② 子系统版本号刷新（检查台 v1.5 / 故事结构 v2.2 / 状态切片 v1.3 / 经络 v1.3 / 世界卡人物卡 v1.1）；③ 退役"统一 canon 访问层待写"洞（已为现行 v1）；④ **导航模型按 ★1 活画布合一更新**：hub②家 + 活画布(③②④⑧相机档·远/中/钻入) + 三辐条⑦ + 独立屏(①⑤⑥⑬)，「4 台」口径被合一取代；⑤ 补 UI 蓝图层/治理层/开发规划层指针；⑥ 重写「当前状态与续接点」到 v2.24（九屏三件齐 / 活画布合一★1+声音★2 已裁决 / P0 根治卡 / 防降智治理卡 / 开发规划总装 M1a-M1b / M0-1 已建）。

## 0 · 先读这个（唯一 canon 指针）
**改任何功能前，先读 `叙引_现行真相源索引_v2.24`。** 它逐层指明该层的现行 spec、状态、已收编谁。**不在索引"现行真相源"列里的文档 = 非现行，不得据以开发。** 本 CLAUDE.md 只是索引的快捷入口 + 铁律速查。

## 是什么
AI 驱动的长篇小说工业化创作平台，内核是**情绪工程引擎**（情绪是目的，文字/媒介是载体）。
小说是整条 IP 业务链（小说→动漫→影视→游戏）的**第一步**；真正的产品是**媒介无关的 IP 母本**，小说只是它的第一张皮。
平台用**七问粒度 + 控制向下**从根上消灭叙事漂移与降智。

## 技术栈（轻栈 + 重持久化）
- React + Vite + TypeScript；单页 + `currentView` 状态切换，**不引入任何路由库**
- 状态：Zustand（集中 store）
- 持久化：Dexie（IndexedDB），藏在仓储接口后
- 运行时 AI：OpenAI 兼容、配置驱动（`.env`：`LLM_BASE_URL/API_KEY/MODEL`），经 Vite 中间件瘦代理；无 key 回退 `StubAIService`

## 工程铁律（技术地基，不可违反）
1. **持久化藏在仓储接口后**：业务只调 `repo.save/get/query/list/delete`，**永不直接 import Dexie 或碰 localStorage**。
2. **创作资产用注册表（schema 驱动）**：加模块 = 注册一条配置，用通用 `assets/registry/projects` 表，**不为每个模块建专属表、不加路由**。
3. **单页 + currentView**：视图切换用全局状态，不引路由库。
4. **资产工作室级共享、项目引用非复制**；连载用宪法快照锁版本。
5. **能数的交代码、要判断的交 LLM**：强制约束靠代码闸，prompt 是概率、做不了强制。
6. **同一数据两边共用同一取数函数**；canon 读取走统一访问层。

## 创作铁律（口径 · 写任何创作功能前必须遵守）
1. **方向自上而下命令**：总纲/分卷纲/章纲由**主干方向推演**逐层「提议→批准→冻结」、命令到达态（entry/exit）；**推演台只在章纲命令的 entry→exit 内填场景，不决定方向**。方向从点的拍板里冒上来 = 宏观叙事漂移，**禁止**。
2. **控制向下、不跨层**：每层只接上层翻译过的指令、只管直接下层；**母题不下到点**（防说教），写手只拿 beats。
3. **情绪走母题的路**：情绪不作为「生情绪命令」下到点；情绪弧留总纲当北极星（进母本），逐层翻成**情绪载体 beat**（状态可验收）下到章纲，软判交审校，火候归涌现。
4. **母本是一等产物、增量沉淀**：媒介无关 IP 母本（每条标「机制/外衣」，**九条为权威**）；L1 冻结总纲时初始化稳定核，推演沉淀涌现壳；**母本层与皮层分存**，可导出。
5. **量是结果非输入**：字数自下而上涌现（逼 = 注水）；章数 top-down 供给目标（靠加真故事达成，不撑章）。
6. **「填满」＝七问达 L2**：以**《七问颗粒度规范 v1.1》§1（锁死项全填）+ §3（放行规则：未达 L2 禁止正式生成）**为权威。锁死"写什么"（谁在场/关系压力/地点限制/对话目的/信息释放/关键行动/结果/缺口），放开"怎么写"（台词/描写/节奏）。新正典只在推演由作者拍板诞生，写手永远只翻译。
7. **根治在上游坍缩（釜底抽薪）**：写作台禁止/自检/闭世界闸只是**兜底保险丝**，不是解药。预防闸 = 七问规范 v1.1 L2 + §7 校验（含**新增"实体闭世界引用校验"**：七问卡引用的命名实体/道具/前史/象征全 trace 到冻结骨骼层正典，否则拦截——P0 manifest 已归位于此，非下游扫描器）。

## 开发节奏
一个任务 → 验收通过 → 再下一个，不一次写一大堆。每个任务结束跑自检验收闸；过关 `git commit`、`PROGRESS.md` 记账。
**提升规则**：任何拍板/验证过的结论，当场并进《现行真相源索引》，否则下个窗口会重推一遍更糙的。

## 参考文档（按《现行真相源索引》分层；改某层只读该层现行 spec）

**A 代 · 认知栈（设计真相源）**
- 宪法：`叙引_第一性原理_情绪工程引擎`
- 完整架构背景全集：`叙引_设计共识总纲_v4.0`
- 系统怎么运转（SSOT）：`叙引_主流程工程规范_三阶段坍缩模型_v2.1`

**A 代 · 工程规格③链（M1 实现真相源 · 彼此承接）**
- 数据模型：`叙引_数据结构总表_第一~四篇`（状态切片 **v1.3** / 骨骼+ArcMilestone+emotion_promise波形 / 经络 **v1.3** / 场景分镜；＋单文件 `数据结构总表_v1.0` 总览）
- **写作颗粒度闸·规则正文**：`叙引_七问颗粒度规范_v1.1`（三档/放行/§7 八校验+实体闭世界校验/AI补全权限）
- 写作台·数据契约：`叙引_写作台输入输出接口_v1.0`
- 检查/审校·数据契约：`叙引_检查台质量审校规范_v1.5`（§5 审校维度 SSOT）
- 事实闸门：`叙引_Story_Reality状态机_v1.0`
- 工程可行性：`叙引_研究型难题Spike方案_v1.0`

**B 代 · 实现分解 + 真新增层（叠加在 A 之上）**
- 方法论（重锚后·只留新内容指针）：`叙引_工程方法论与设计原则_v1`（点线面体/控制向下/釜底抽薪/母本一等产物；锚9填满→指七问规范 v1.1）
- 方向主干：`叙引_主干方向推演引擎_设计规范_v1` + 主干 L1·L3 工程卡
- 推演层：`推演台设计规范_v1.1`（引擎）+ `推演_目的与多轴分档_v1`（内核）+ `推演台工程卡_分档与信息控制轴`（落地）
- 故事结构层：`故事结构层规范_v2.2`（递归起承转合树 + 相位×叙事职能 + 张力曲线塌陷自检）
- 创意派生：`创意派生链_重建蓝图_v1` + 阶段0-5卡（+ `双层世界修正卡` 补丁，待并入阶段卡）
- 设定层正典治理：`世界卡人物卡字段_v1.1` + `世界卡结构化_v1.1` + `点亮即冻结_v1` + `设定层接线_v1`
- 写作台·创作规则：`写作台规范_v1` + `自检器升级卡_v1`（与 A 代写作台接口合为完整 spec）
- 跨层：`情绪载体beat_v1` + `多维情绪账本_v4.1`
- 工程地基：`叙引_三面承重墙决策书_v1`（持久化仓储接口+Dexie / 资产注册表 / 轻栈，三决策已拍板；含里程碑0地基三件套）
- 远期：`多项目工作室层_v1`；下游媒介层 `导演台v5.2 / 盲区v5.1 / 三位一体`

**C 代 · UI 蓝图层（用户界面真相源 · 九屏三件齐）**
- 框架卷：`统一UI蓝图_框架卷_v1.3`（全局壳/横切§2.9看提示做/11项spec模板+双闸/§4屏清单分批/§6用户旅程）
- 防回退 SOP：`UI逐屏设计_防回退机制+环节契约模板_v1`（设计层防漂移）
- 九屏：①创意评估 ②活世界仪表盘(家·hub) ③世界生长沙盘 ④推演台 ⑤写作台 ⑥审校台 ⑦三辐条 ⑧全局掌控台 ⑬作品蓝本——各三件齐(环节契约+spec+原型)
- 产品层·活世界：`★1 活画布四屏合一决策书 v1(拍C)` + `活画布合一接线卡 v1` + 活世界三动作 spec 工程卡——②③④⑧同画布·相机档·view-state{camera,mode}
- 声音：`★2 声音治理分工卡 v1(拍C)`——角色声音(人物层)/叙述者腔调(书级)两层

**治理层 · 防降智（写任何裁决前必读）**
- `防降智治理卡_决策反向引用闸_v1.1`：裁决落库(gray→gold)附 override_manifest(穷举动了谁的字段/轴·空不合并·同批硬改)+核对闸 grep 旧口径零残留。三面漂移闸(正文颗粒度/屏环节契约/裁决反向引用)合一。
- `文档治理方案+索引状态轴_v1`（5态轴·规范层不合并）

**开发规划层 · 建造主序**
- `开发规划总装_v1.1`：M0地基→M1(M1a引擎内核证对·①fixture④⑤⑥ ⊂ M1b用户主链证好用·①⑬③⑤⑥+黄金三章·≡框架卷§4第一批)→M2+(post-M1闸·决策二自动化胃口拍A/B依M1数据)
- 工程地基已就绪：`三面承重墙决策书_v1` + `里程碑0地基三件套CC任务包_v1.1`

**已闭合（不再是洞）**：统一 canon 访问层 → 现行 `统一canon访问层_v1`（canonStatus-aware·作用域查询）。
**远期·非现行**：`推演引擎_配置家族_远期路线图`——别当现行架构读。

## 当前状态与续接点（2026-06-29 · 对齐索引 v2.24）
- **图纸态**：§3 全闭无实质开口；两个 soul decision（★1 活画布合一 / ★2 声音治理）均拍 C 落定；emotion 四维↔八族字段对齐落地（账本仍四维·零漂移）。九屏（①②③④⑤⑥⑦⑧⑬）三件齐、对齐闸全勾。
- **认知态反推已收窄**（v2.20）：knows/not_knows/misbelieves 在推演冻结、写作只消费、检查做闭世界校验（非反推重建）；四大 spike 里风险最高的「正文反推状态」降为可硬判 membership 校验。
- **防降智上线**（v2.21）：今后任何裁决走治理卡决策双闸（override_manifest + 核对闸）。
- **开发规划已定**（v2.23/v2.24·决策一拍A）：M0地基→M1(M1a引擎内核⊂M1b用户主链)→M2+(post-M1闸)；决策二（自动化胃口）待 M1 跑出 spike 数据后拍。
- **已开建（代码）**：**M0-1 应用骨架完成**（view-state{camera,mode}+活画布相机档+三辐条正交+无路由库+build/lint 过·已 commit+push 到 branch `claude/initial-setup-zSwad`）。
- **当前续接点**：**M0-2 持久化层**（Repository 接口 + Dexie 实现·铁律1）→ M0-3 资产注册表（AssetSchema 含 canonStatus·字段源=数据四篇+P0 SceneBeat）。M0-3 前需把数据四篇+P0根治卡补进 repo `docs/canon/`。
- **验收**：M0 总闸=存取 200 章规模结构化数据不崩；M1 闭环=一章系统真跑·零漂移可测·四 spike 各采一数据点。
- **主战场**：从"补文档/对齐"转入"接线开发"，M0→M1 主序执行中。
